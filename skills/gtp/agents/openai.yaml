interface:
  display_name: "GitHub Task Protocol"
  short_description: "後から変更しにくい重要判断をDecision Recordへ記録する"
  default_prompt: "Use $gtp to record only consequential decisions whose selected means are not already determined and would be costly to change later."
