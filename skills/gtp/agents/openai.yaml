interface:
  display_name: "GitHub Task Protocol"
  short_description: "重要な判断を記録し、Issue・PRを提出前reviewする"
  default_prompt: "Use $gtp to record consequential decisions and run the pre-submission review for an Issue or pull request; infer its language from user-authored text or repository context, and ask if neither selects one."
